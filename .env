# Committed placeholders so the Prisma CLI can resolve variables.
# Real values go in .env.local (git-ignored), which overrides these at runtime.
# Do NOT put real secrets here.
DATABASE_URL="postgresql://user:password@localhost:5432/obsidian_club?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/obsidian_club?schema=public"
