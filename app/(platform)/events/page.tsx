import ComingSoon from "@/components/shared/ComingSoon";

// Real events are BACKLOG.md's v0.7 — placeholder only, see
// app/(platform)/rooms/page.tsx.
export default function EventsPage() {
  return (
    <ComingSoon
      title="Events"
      note="Closed gatherings — dinners, parties, journeys. Opening in a future version."
    />
  );
}
