import ComingSoon from "@/components/shared/ComingSoon";

// Real content/library is BACKLOG.md's v0.6 — placeholder only, see
// app/(platform)/rooms/page.tsx.
export default function ContentPage() {
  return (
    <ComingSoon
      title="Library"
      note="Essays, lectures, manifestos, courses. Opening in a future version."
    />
  );
}
