// PLACEHOLDER (2026-09-10, see DECISIONS.md) — what the landing page
// shows in LaunchCountdown's slot once the countdown reaches zero.
// Max is deciding the exact post-launch treatment separately; until
// then this renders nothing. Swap the return value below for the real
// content when it's ready — nothing about LaunchCountdown itself needs
// to change.
export default function LaunchCountdownPlaceholder() {
  return null;
}
